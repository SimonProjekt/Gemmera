# GnuPG passphrase recipe

Stored in the encrypted vault under `secrets/gpg-passphrase.kdbx`. The recovery word list is taped inside the lid of the small wooden box on the bookshelf.

GnuPG keyring lives at `~/.gnupg/` on the laptop.

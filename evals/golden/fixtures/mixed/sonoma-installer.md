# macOS Sonoma installer build script

Located at `scripts/build-sonoma-installer.sh`. Uses `createinstallmedia` from the downloaded Sonoma app bundle. Target volume must be erased and formatted as Mac OS Extended (Journaled) before the script will proceed.

Run as root from outside the target volume.
